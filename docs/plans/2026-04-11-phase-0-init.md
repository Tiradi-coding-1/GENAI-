# Phase 0: 專案初始化與環境建立 Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** 建立完整的 Python 專案環境，包含資料夾結構、依賴套件與 Git 版本控制

**Architecture:** 標準 Python 專案結構，使用 requirements.txt 管理依賴

**Tech Stack:** Python 3.12+, pip

---

## Task 1: 建立資料夾結構

**Files:**
- Create: `src/`
- Create: `data/`
- Create: `charts/`
- Create: `tests/`
- Create: `docs/`

**Step 1: 建立資料夾**

```bash
mkdir -p src data charts tests docs
```

**Step 2: 驗證資料夾存在**

```bash
ls -la
```

Expected: 5 個資料夾存在

---

## Task 2: 生成 requirements.txt

**Files:**
- Create: `requirements.txt`

**Step 1: 寫入 requirements.txt**

```txt
yfinance
pandas
numpy
mplfinance
matplotlib
```

**Step 2: 驗證格式正確**

```bash
cat requirements.txt
```

Expected: 5 行，包含所有必要套件

---

## Task 3: 生成 .gitignore

**Files:**
- Create: `.gitignore`

**Step 1: 寫入 .gitignore**

```gitignore
__pycache__/
.env
data/
charts/
*.pyc
.DS_Store
```

**Step 2: 驗證格式正確**

```bash
cat .gitignore
```

Expected: 包含所有必要規則

---

## Task 4: 完成 Git 初始化

**Files:**
- Modify: 已完成的 initial commit

**Step 1: 驗證 Git 狀態**

```bash
git status
```

Expected: 只有 .gitignore, requirements.txt, 資料夾 untracked

**Step 2: Add 並 commit**

```bash
git add .gitignore requirements.txt && git commit -m "feat: 完成 Phase 0 - 專案初始化與環境建立"
```

---

## Task 5: 安裝依賴套件

**Step 1: 安裝 requirements.txt**

```bash
pip install -r requirements.txt
```

**Step 2: 驗證安裝成功**

```bash
pip list | grep -E "yfinance|pandas|numpy|mplfinance|matplotlib"
```

Expected: 5 個套件都有版本顯示